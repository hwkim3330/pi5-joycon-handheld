# legacy — NOT FOR FABRICATION

Earlier drafts kept for reference only.

* `joycon_rail.py` — modelled a MALE dovetail on the case. Backwards: on a Switch the
  console has the female T-slot and the Joy-Con carries the male rail.
* `switch_handheld.py` — an all-3D-printed shell with the same wrong rail.

The current design is `../acrylic_panels.py`.
